# Role
你是一位資深全端工程師，擅長使用原生 JavaScript (Vanilla JS) 進行高效能 Web 開發，並精通 Google Maps API 整合與 Tailwind CSS 響應式設計。

# Task
請幫我開發一個單一檔案的 Web App（index.html），功能名稱為「120 分鐘美味雷達」。
這個工具必須使用「純原生 JavaScript」撰寫，不使用任何前端框架。

# Data & Security
- Google API Key: AIzaSyA-W47DliyOJbMn9NXfFQbzMJ39yVWjz-c
- 必須將此 Key 填入 script 標籤中：https://maps.googleapis.com/maps/api/js?key=AIzaSyA-W47DliyOJbMn9NXfFQbzMJ39yVWjz-c&libraries=places

# Functional Requirements
1. **手動位置搜尋**:
   - 建立一個搜尋框，整合 google.maps.places.Autocomplete。
   - 僅限手動輸入，不使用瀏覽器自動定位。
2. **餐廳過濾與 120 分鐘邏輯 (核心)**:
   - 搜尋半徑：2000 公尺。
   - 判定邏輯：計算「當前時間 + 120 分鐘」，比對餐廳在該時段是否營業，需處理跨午夜判定。
   - 成本優化：先過濾評價與價格，再對前 15 筆進行 getDetails 檢查營業時間。
3. **資料排序**:
   - 最終結果依據 rating 由高到低排序，僅顯示前 10 筆。

# UI/UX Requirements (重點更新)
1. **配色方案 (食指大動的暖色系)**:
   - 請放棄冷色調。全面使用暖色系 Tailwind 類別以營造溫暖、美味的氛圍。
   - 主色調建議使用 `orange` (橘色) 或 `red` (紅色) 系列。背景使用暖白色 (如 `bg-amber-50`)。
   - 按鈕、標題應使用高飽和度的暖色來刺激食慾。
2. **RWD 響應式設計 (Mobile-First)**:
   - 介面必須是行動裝置優先設計。
   - **手機版 (< md)**: 搜尋框、評價滑桿、價格選擇器垂直堆疊，卡片為單欄顯示。
   - **平板/桌面版 (>= md)**: 搜尋區域改為橫向並排佈局 (使用 Flex 或 Grid)，更有效利用寬度。餐廳卡片在較大螢幕上可以使用雙欄網格排列。
   - 確保所有輸入元件在手機上都夠大，易於觸控。
3. **元件細節**:
   - 餐廳卡片設計要誘人，像菜單一樣。顯示：店名、星等(使用黃色星星圖示)、評論數、價格等級、營業狀態標籤。
   - 包含溫暖色調的 Loading 動畫與錯誤提示。

# Deliverables
請提供完整的單一檔案 index.html 原始碼，並附帶詳細的中文註釋。